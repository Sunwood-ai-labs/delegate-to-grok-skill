interface:
  display_name: "Delegate to Grok Build"
  short_description: "Safe Grok Build delegation and public X research"
  default_prompt: "Use $delegate-to-grok to perform a read-only Grok Build investigation with direct sources."
